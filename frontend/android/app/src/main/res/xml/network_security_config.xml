<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Development only: allow HTTP calls to the local backend while testing on Android. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
